<div class="card h-100">
  <div class="card-body">
    <h5 class="card-title"><%= movie.title %></h5>
    <p class="card-text"><%= movie.director %> — <%= movie.release_year %></p>
    <p><span class="badge bg-secondary"><%= movie.rating %></span></p>

    <%= link_to "Show", movie_path(movie), class: "btn btn-sm btn-outline-primary" %>
    <%= link_to "Edit", edit_movie_path(movie), class: "btn btn-sm btn-outline-warning" %>
    <%= link_to "Delete", movie_path(movie), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-sm btn-outline-danger" %>
  </div>
</div>
