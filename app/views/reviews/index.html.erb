<h1>Reviews for <%= @movie.title %></h1>

<p>
  <%= link_to "Back to Movie", movie_path(@movie) %> |
  <%= link_to "New Review", new_movie_review_path(@movie) %>
</p>

<ul>
  <% @reviews.each do |review| %>
    <li>
      <%= link_to "#{review.reviewer} (#{review.rating})", movie_review_path(@movie, review) %>
      — <%= truncate(review.body, length: 80) %>
    </li>
  <% end %>
</ul>