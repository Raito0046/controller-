<!DOCTYPE html>
<html>
  <head>
    <title>MovieApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-..." crossorigin="anonymous">

    <!-- Custom CSS -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <style>
      :root {
        --brand-primary: #2b6cb0; 
        --brand-accent: #f6ad55;  
      }
      .bg-brand {
        background-color: var(--brand-primary) !important;
        color: white !important;
      }
      .text-accent {
        color: var(--brand-accent) !important;
      }
    </style>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-brand">
      <div class="container">
        <a class="navbar-brand text-white" href="<%= root_path %>">MovieApp</a>
        <div>
          <a class="nav-link d-inline text-white" href="<%= movies_path %>">Movies</a>
          <a class="nav-link d-inline text-white" href="<%= new_movie_path %>">Add Movie</a>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <% flash.each do |key, msg| %>
        <div class="alert alert-<%= key == 'notice' ? 'success' : 'danger' %>"><%= msg %></div>
      <% end %>

      <%= yield %>
    </div>

    <!-- Bootstrap JS (CDN) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-..." crossorigin="anonymous"></script>
  </body>
</html>
